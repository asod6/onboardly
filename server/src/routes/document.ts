import express from "express"; import { createDocument, getDocuments, getDocument, updateDocument, deleteDocument } from "../controllers/document"; import { auth } from "../middleware/auth"; const router = express.Router(); router.post("/", auth, createDocument); router.get("/", auth, getDocuments); router.get("/:id", auth, getDocument); router.patch("/:id", auth, updateDocument); router.delete("/:id", auth, deleteDocument); export default router;
