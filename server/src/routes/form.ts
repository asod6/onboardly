import express from "express"; import { createForm, getForms, getForm, updateForm, deleteForm } from "../controllers/form"; import { auth } from "../middleware/auth"; const router = express.Router(); router.post("/", auth, createForm); router.get("/", auth, getForms); router.get("/:id", auth, getForm); router.patch("/:id", auth, updateForm); router.delete("/:id", auth, deleteForm); export default router;
