import mongoose, { Schema } from "mongoose"; export interface IForm extends mongoose.Document { title: string; fields: Array<{ name: string; type: string; required: boolean }>; userId: mongoose.Types.ObjectId; createdAt: Date; updatedAt: Date; } const formSchema = new Schema({ title: { type: String, required: true, trim: true }, fields: [{ name: { type: String, required: true }, type: { type: String, required: true, enum: ["text", "number", "date", "select"] }, required: { type: Boolean, default: false } }], userId: { type: Schema.Types.ObjectId, ref: "User", required: true }, createdAt: { type: Date, default: Date.now }, updatedAt: { type: Date, default: Date.now } }); formSchema.pre("save", function(next) { this.updatedAt = new Date(); next(); }); export const Form = mongoose.model<IForm>("Form", formSchema);
