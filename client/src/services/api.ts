import axios from "axios"; const API_URL = process.env.REACT_APP_API_URL || "http://localhost:5000/api"; const api = axios.create({ baseURL: API_URL, headers: { "Content-Type": "application/json" } }); api.interceptors.request.use((config) => { const token = localStorage.getItem("token"); if (token) { config.headers.Authorization = `Bearer ${token}`; } return config; }); export const documentService = { getAll: () => api.get("/documents"), getOne: (id: string) => api.get(`/documents/${id}`), create: (data: any) => api.post("/documents", data), update: (id: string, data: any) => api.patch(`/documents/${id}`, data), delete: (id: string) => api.delete(`/documents/${id}`) }; export const formService = { getAll: () => api.get("/forms"), getOne: (id: string) => api.get(`/forms/${id}`), create: (data: any) => api.post("/forms", data), update: (id: string, data: any) => api.patch(`/forms/${id}`, data), delete: (id: string) => api.delete(`/forms/${id}`) }; export default api;
